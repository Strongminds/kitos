apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: aln-test-db-backup-disabled
  namespace: kitos
spec:
  suspend: true
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: aln-test-db