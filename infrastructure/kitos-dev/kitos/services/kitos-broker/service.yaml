apiVersion: v1
kind: Service
metadata:
  name: kitos-broker
  namespace: kitos
spec:
  ports:
  - appProtocol: http
    name: http
    port: 80
    protocol: TCP
    targetPort: 5672
  selector:
    app: kitos-broker
  type: ClusterIP
