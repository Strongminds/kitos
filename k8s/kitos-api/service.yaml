apiVersion: v1
kind: Service
metadata:
  name: kitos-api
  namespace: kitos
  labels:
    app.kubernetes.io/name: kitos-api
    app.kubernetes.io/part-of: kitos
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: kitos-api
