name: Deploy Kitos PubSub

env:
    RABBIT_MQ_USERNAME: ${{ secrets.RABBIT_MQ_USERNAME }}
    RABBIT_MQ_PASSWORD: ${{ secrets.RABBIT_MQ_PASSWORD }}
    PUBSUB_API_KEY: ${{ secrets.PUBSUB_API_KEY }}
    DOCKER_REPO: kitos-pubsub
    DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
    DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
    
on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        type: choice
        default: 'Staging'
        options:
          - 'Development'
          - 'Staging'
          - 'Production'
  
jobs:
  build-and-push-docker-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Login to Docker Hub
        run: echo "${{ env.DOCKER_PASSWORD }}" | docker login -u "${{ env.DOCKER_USERNAME }}" --password-stdin
      
      - name: Set Environment Variables 
        run: | 
          if [ "${{ github.event.inputs.environment }}" == "Staging" ]; then 
            echo "SERVER=${{ secrets.STAGING_PUBSUB_SERVER }}" >> $GITHUB_ENV 
            echo "TAG=staging" >> $GITHUB_ENV
            echo "IDP_HOST_MAPPING=${{ secrets.STAGING_IDP_HOST_MAPPING }}" >> $GITHUB_ENV
            echo "DEPLOY_USER=${{ secrets.STAGING_DEPLOY_USER }}" >> $GITHUB_ENV
          fi 

      - name: Build and push PubSub API Docker image
        run:  sh PubSub.Application/Deployment/build_and_push_docker_image.sh ${{ env.DOCKER_USERNAME }} ${{ env.DOCKER_REPO }} ${{ env.TAG }} 
      
      - name: deploy-pubsub-api
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ env.SERVER }} 
          username: ${{ env.DEPLOY_USER }} 
          key: ${{ secrets.STAGING_SSH_PRIVATE_KEY }}
          script: | 
            cd /home/kitosadmintest/app
            sudo sh run_in_docker_compose.sh ${{ github.event.inputs.environment }} ${{ env.RABBIT_MQ_USERNAME }} ${{ env.RABBIT_MQ_PASSWORD }} ${{ env.PUBSUB_API_KEY }} ${{ env.IDP_HOST_MAPPING }}

